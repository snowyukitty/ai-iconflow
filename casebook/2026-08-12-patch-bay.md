---
slug: patch-bay
date: 2026-08-12
project: Patch Bay Merge
targets: web
essence: route
style_family: flat-geometric
signature_device: three keyed socket mouths visibly join one continuous trunk
device_family: object-silhouette
device_detail: three keyed socket mouths visibly join one continuous trunk
concept_lens: specific-object
cliche_avoided: generic nodes, USB logo, cable crossing, hub monogram
status: shipped
scores_first: legibility=3 distinctiveness=3 balance=4 color=4 scalability=4 craft=4
scores_final: legibility=4 distinctiveness=4 balance=4 color=4 scalability=4 craft=4
iterations: 2
---

## Summary
Patch Bay Merge serves the job “route several hardware tools into one tested bench workflow”. The selected patch bay direction won because its silhouette stays specific while three keyed socket mouths visibly join one continuous trunk carries the single signature device.

## What failed first
<!-- What the earlier passes got wrong and which change fixed it. This is the
     raw material for future lessons — be specific (axis, size, shape). -->

## Lessons
<!-- One reusable rule per bullet. `- [ ]` = not yet distilled into the docs;
     flip to `- [x]` after promoting it (see docs/EVOLUTION.md). -->
- [x] At 16px, preserve the patch bay before medium-specific detail; the winning lens was Object lens — a three-socket bench patch bay with keyed mouths.
