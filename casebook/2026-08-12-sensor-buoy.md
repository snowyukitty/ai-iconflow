---
slug: sensor-buoy
date: 2026-08-12
project: Sensor Buoy
targets: web
essence: report
style_family: line-mark
signature_device: the anchor line returns into the buoy body as one broad reporting loop
device_family: path-relationship
device_detail: the anchor line returns into the buoy body as one broad reporting loop
concept_lens: specific-object
cliche_avoided: wifi bars, antenna tower, map pin, cloud, generic status dot
status: shipped
scores_first: legibility=3 distinctiveness=3 balance=4 color=4 scalability=4 craft=4
scores_final: legibility=4 distinctiveness=4 balance=4 color=4 scalability=4 craft=4
iterations: 2
---

## Summary
Sensor Buoy serves the job “see whether a remote field sensor is anchored and reporting”. The selected buoy direction won because its silhouette stays specific while the anchor line returns into the buoy body as one broad reporting loop carries the single signature device.

## What failed first
<!-- What the earlier passes got wrong and which change fixed it. This is the
     raw material for future lessons — be specific (axis, size, shape). -->

## Lessons
<!-- One reusable rule per bullet. `- [ ]` = not yet distilled into the docs;
     flip to `- [x]` after promoting it (see docs/EVOLUTION.md). -->
- [x] At 16px, preserve the buoy before medium-specific detail; the winning lens was Object lens — a moored instrument buoy with a short mast and weighted keel.
